#pragma once

/**
 * @typedef int8_t
 * @brief Signed 8-bit integer.
 */
typedef __INT8_TYPE__ int8_t;

/**
 * @typedef int16_t
 * @brief Signed 16-bit integer.
 */
typedef __INT16_TYPE__ int16_t;

/**
 * @typedef int32_t
 * @brief Signed 32-bit integer.
 */
typedef __INT32_TYPE__ int32_t;

/**
 * @typedef int64_t
 * @brief Signed 64-bit integer.
 */
typedef __INT64_TYPE__ int64_t;

/**
 * @typedef uint8_t
 * @brief Unsigned 8-bit integer.
 */
typedef __UINT8_TYPE__ uint8_t;

/**
 * @typedef uint16_t
 * @brief Unsigned 16-bit integer.
 */
typedef __UINT16_TYPE__ uint16_t;

/**
 * @typedef uint32_t
 * @brief Unsigned 32-bit integer.
 */
typedef __UINT32_TYPE__ uint32_t;

/**
 * @typedef uint64_t
 * @brief Unsigned 64-bit integer.
 */
typedef __UINT64_TYPE__ uint64_t;

/**
 * @typedef int_least8_t
 * @brief Smallest-width signed integer type with at least 8 bits.
 */
typedef __INT_LEAST8_TYPE__ int_least8_t;

/**
 * @typedef int_least16_t
 * @brief Smallest-width signed integer type with at least 16 bits.
 */
typedef __INT_LEAST16_TYPE__ int_least16_t;

/**
 * @typedef int_least32_t
 * @brief Smallest-width signed integer type with at least 32 bits.
 */
typedef __INT_LEAST32_TYPE__ int_least32_t;

/**
 * @typedef int_least64_t
 * @brief Smallest-width signed integer type with at least 64 bits.
 */
typedef __INT_LEAST64_TYPE__ int_least64_t;

/**
 * @typedef uint_least8_t
 * @brief Smallest-width unsigned integer type with at least 8 bits.
 */
typedef __UINT_LEAST8_TYPE__ uint_least8_t;

/**
 * @typedef uint_least16_t
 * @brief Smallest-width unsigned integer type with at least 16 bits.
 */
typedef __UINT_LEAST16_TYPE__ uint_least16_t;

/**
 * @typedef uint_least32_t
 * @brief Smallest-width unsigned integer type with at least 32 bits.
 */
typedef __UINT_LEAST32_TYPE__ uint_least32_t;

/**
 * @typedef uint_least64_t
 * @brief Smallest-width unsigned integer type with at least 64 bits.
 */
typedef __UINT_LEAST64_TYPE__ uint_least64_t;

/**
 * @typedef int_fast8_t
 * @brief Fastest signed integer type with at least 8 bits.
 */
typedef __INT_FAST8_TYPE__ int_fast8_t;

/**
 * @typedef int_fast16_t
 * @brief Fastest signed integer type with at least 16 bits.
 */
typedef __INT_FAST16_TYPE__ int_fast16_t;

/**
 * @typedef int_fast32_t
 * @brief Fastest signed integer type with at least 32 bits.
 */
typedef __INT_FAST32_TYPE__ int_fast32_t;

/**
 * @typedef int_fast64_t
 * @brief Fastest signed integer type with at least 64 bits.
 */
typedef __INT_FAST64_TYPE__ int_fast64_t;

/**
 * @typedef uint_fast8_t
 * @brief Fastest unsigned integer type with at least 8 bits.
 */
typedef __UINT_FAST8_TYPE__ uint_fast8_t;

/**
 * @typedef uint_fast16_t
 * @brief Fastest unsigned integer type with at least 16 bits.
 */
typedef __UINT_FAST16_TYPE__ uint_fast16_t;

/**
 * @typedef uint_fast32_t
 * @brief Fastest unsigned integer type with at least 32 bits.
 */
typedef __UINT_FAST32_TYPE__ uint_fast32_t;

/**
 * @typedef uint_fast64_t
 * @brief Fastest unsigned integer type with at least 64 bits.
 */
typedef __UINT_FAST64_TYPE__ uint_fast64_t;

/**
 * @typedef intptr_t
 * @brief Signed integer type capable of holding a pointer.
 */
typedef __INTPTR_TYPE__ intptr_t;

/**
 * @typedef uintptr_t
 * @brief Unsigned integer type capable of holding a pointer.
 */
typedef __UINTPTR_TYPE__ uintptr_t;
